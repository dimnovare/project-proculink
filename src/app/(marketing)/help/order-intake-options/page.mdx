export const metadata = {
  title: "Ways to send orders to ProcuLink — ProcuLink Help",
  description: "The supported and assisted ways buyer teams can send purchase orders into ProcuLink.",
};

# Ways to send orders to ProcuLink

ProcuLink is designed to receive purchase orders the way your buyer or procurement team already works. Start with upload for the first supplier, then move recurring flows to email, API, or a watched folder when the mapping is proven.

## Recommended order

1. **Manual upload** — best for the first supplier setup and testing real files.
2. **Email intake** — best when buyers already send POs as attachments.
3. **API push** — best when an ERP, procurement system, or automation tool can send structured order data.
4. **SFTP or storage-folder polling** — best for recurring file exports from older systems. This is currently an assisted setup.

## Manual upload

Use **Upload** in the ProcuLink app when you want immediate control. This is the safest first path because you can pick the supplier, review the parsed order, fix mappings, and confirm the supplier-ready output before delivery.

Common PO formats include CSV, XLSX, text-based PDF, XML/cXML, UBL/Peppol-style XML, EDIFACT, and X12. Scanned/image-only PDFs need OCR support and should be treated as an assisted flow until enabled for your workspace.

## Email intake

There are two email patterns:

- **IMAP polling**: ProcuLink checks a mailbox you own every 5 minutes and imports supported attachments. This is configured in **Settings → Email intake**.
- **Hosted inbound address**: ProcuLink can receive messages through a provider-backed inbound email webhook, using an address such as `orders@your-workspace.proculink.eu`. This is an assisted setup while custom routing is being finalized.

For repeatable automation, use a dedicated mailbox or folder and ask senders to attach one purchase order per email.

## API push

Use the inbound API when another system can send structured order data directly. The API is authenticated with a ProcuLink API key and scoped to your workspace slug.

This path is best for ERPs, procurement systems, Zapier/Make-style automation, or internal tools that already know the supplier and line-item data.

## SFTP or storage-folder polling

ProcuLink can poll external SFTP and S3-compatible storage locations for recurring PO files. These flows are useful when a source system exports files on a schedule.

Because folder-based intake needs supplier-resolution rules, duplicate protection, and credentials, it is currently configured with ProcuLink support rather than fully self-service.

## What to send

Whichever channel you use, the best files include:

- Purchase order number
- Order date
- Supplier name or supplier account reference
- Currency
- One row per ordered item
- Buyer item code or supplier item code
- Description, quantity, unit, and price when available

If a file is missing fields, ProcuLink will route it to review instead of silently sending a risky order.

---

Need help choosing the right intake path? Email [support@proculink.eu](mailto:support@proculink.eu) or start with [your first purchase order upload](/help/first-upload).
