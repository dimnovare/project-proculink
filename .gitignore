# Environment variables — .env is committed (public defaults), .env.local is secret
.env.local
.env.*.local

# Logs
logs
*.log

# Live-matrix harness RUN ARTIFACTS (the harness tooling itself IS committed;
# its accumulated run output is not — it's resumable local state, not source).
scripts/live-matrix/ingest-results.ndjson
scripts/live-matrix/*-results.ndjson
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.next
out
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
obj/
/.vs/
tsconfig.tsbuildinfo

# Local UI QA screenshots
.qa-screenshots/

# Playwright test artifacts
test-results/
playwright-report/

# clerk configuration (can include secrets)
/.clerk/
.vercel

# Playwright scratch output from the guide screenshot rig (bun run guides:capture).
# The screenshots themselves are committed under public/guides/.
scripts/guide-shots/.playwright/

# Generated Task 1 film captures and production artifacts.
scripts/demo-video/films/out/
